# .dockerignore for frontend-pos

# Node modules
node_modules/

# Build output (if any exists locally, the Docker build will create its own)
dist/
build/

# Git files
.git
.gitignore

# Docker files (already in context)
Dockerfile
.dockerignore

# Environment files (these should be passed at runtime, not baked into image)
.env
.env.*
!*.env.example

# Editor/OS specific
.vscode/
.idea/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*